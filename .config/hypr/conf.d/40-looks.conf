# Pull accent colors from pywal exports where convenient
# Waybar uses ~/.config/waybar/wal.css include
# For Hyprland, set active/inactive border via wal-generated colors (optional)
# You can script-inject below during theme apply if desired.


general {
col.active_border = rgba(88c0d0ee)
col.inactive_border = rgba(4c566aee)
}